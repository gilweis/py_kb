# Usage:
```
usage: py_kb.py [-h] --infile INFILE --win_title WIN_TITLE
                [--interval INTERVAL]

pyget_chain

options:
  -h, --help            show this help message and exit
  --infile INFILE       Input filename
  --win_title WIN_TITLE
                        Window title
  --interval INTERVAL   interval
```